{
  "name": "eldercare-companion",
  "version": "1.0.0",
  "description": "AI-powered elderly care companion web application",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["eldercare", "ai", "healthcare", "companion"],
  "author": "ElderCare Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
